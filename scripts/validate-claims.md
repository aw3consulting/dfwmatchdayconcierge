# validate-claims

Governed script specification for validating that all public claims map to approved capability statuses in `data/claims.json` and `data/services.json`.

Expected result: claim validation passed.
