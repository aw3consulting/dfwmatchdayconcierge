# DFW Homepage v1.0.4 Governance-to-Execution Handoff and Output Contract v1.0.0

## Controlled Action

`dfw_homepage_v1_0_4_governance_to_execution_handoff_and_output_contract`

## Status

`LOCKED / GOVERNANCE-TO-EXECUTION HANDOFF CONTRACT CREATED / V1.0.4 CODE STILL BLOCKED UNTIL USER AUTHORIZATION AND PRE-CODE TABLE PASS`

## Purpose

This contract closes the remaining gap between locked governance and the acting execution session.

The next acting session may not interpret the governance stack freely. It must execute the locked v1.0.4 homepage target through a fixed handoff sequence, fixed output contract, fixed file authority, fixed review path, and fixed stopping rules.

## Execution Role

The acting session must operate as:

`DFW HOMEPAGE V1.0.4 MINIMAL PREMIUM GATEWAY EXECUTION OPERATOR`

This role includes:

1. source verifier
2. pre-code gatekeeper
3. homepage content boundary enforcer
4. minimal premium UX executor
5. browser prototype builder
6. claim-safety reviewer
7. sponsor-commercial-value reviewer
8. responsive frontend reviewer
9. GitHub commit verifier

If any role would reject the work, execution must stop.

## Required Opening Sequence

The acting session must complete this sequence before editing code:

1. Verify the required governance stack on `main`.
2. Confirm the v1.0.4 wireframe and build brief is active.
3. Confirm the v1.0.4 copy lock is active.
4. Complete the pre-code section acceptance table.
5. Return the table for review.
6. Wait for user authorization if required by the user.
7. Only after authorization, create or edit the v1.0.4 prototype files.

## Required First Output

The acting session's first substantive output must be the pre-code section acceptance table.

It must not begin with code, CSS, HTML, mockups, PDFs, screenshots, or visual claims.

The table must include:

1. section name
2. homepage function served
3. exact copy or copy limit
4. CTA
5. linked destination
6. sponsor treatment
7. forbidden content removed
8. pass or fail status

Every row must pass before code begins.

## File Authority

Allowed files for v1.0.4 execution:

1. `docs/prototypes/homepage-v1.0.4/review.html`
2. `docs/prototypes/homepage-v1.0.4/assets/css/tokens.css`
3. `docs/prototypes/homepage-v1.0.4/assets/css/prototype.css`
4. `docs/prototypes/homepage-v1.0.4/assets/css/preview-fixes.css`
5. `docs/prototypes/homepage-v1.0.4/README.md` only if needed for review notes

Blocked files during v1.0.4 execution:

1. production homepage files
2. linked page files
3. Batch 1 mockup files
4. PDF files
5. image files
6. DreamHost deployment files
7. QR destination files
8. global governance files unless the user separately authorizes a governance correction

## Output Contract

The acting session must return only these execution outputs after authorized code work:

1. exact files created or edited
2. commit SHA or SHAs
3. preview URL
4. source verification status
5. pre-code table status
6. browser review readiness status
7. known limitations, if any
8. final standing

The acting session must not claim approval.

Only the user may approve the browser prototype.

## Stopping Rules

The acting session must stop if:

1. a required governance file is missing from `main`
2. the pre-code table has any failed row
3. user authorization has not been given for code execution
4. any unauthorized file path needs editing
5. the copy lock cannot be preserved
6. the section order cannot be preserved
7. the prototype would repeat a prior v1.0.1, v1.0.2, or v1.0.3 failure pattern
8. the design requires a new governance decision

## No Self-Approval Rule

The acting session may mark the prototype as ready for browser review.

The acting session may not mark the prototype as approved.

The acting session may not proceed to PDF capture unless the user explicitly approves the browser review.

## Final Determination

`GOVERNANCE-TO-EXECUTION HANDOFF LOCKED / ACTING SESSION MUST START WITH PRE-CODE TABLE / CODE OUTPUT CANNOT SELF-APPROVE / PDF CAPTURE REMAINS BLOCKED UNTIL USER APPROVES BROWSER REVIEW`
