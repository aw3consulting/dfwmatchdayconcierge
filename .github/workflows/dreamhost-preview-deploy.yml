name: DreamHost Preview Deploy

on:
  workflow_dispatch:
    inputs:
      confirm_preview_deploy:
        description: "Type DEPLOY_PREVIEW to deploy the protected DreamHost preview path"
        required: true
        type: string
  push:
    branches:
      - main
    paths:
      - .github/workflows/dreamhost-preview-deploy.yml
      - .github/dreamhost-preview-deploy-request.txt
      - scripts/dreamhost-preview-deploy-remote.sh

permissions:
  contents: read

env:
  LIVE_ROOT: /home/dfw_webadmin/preview.dfwmatchdayconcierge.com
  STAGING_ROOT: /home/dfw_webadmin/staging.dfwmatchdayconcierge.com
  PREVIEW_PATH: preview

jobs:
  deploy-preview:
    name: Deploy protected DreamHost preview
    runs-on: ubuntu-latest
    environment: dreamhost-preview
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Require deployment authorization
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            test "${{ github.event.inputs.confirm_preview_deploy }}" = "DEPLOY_PREVIEW"
          else
            test -f .github/dreamhost-preview-deploy-request.txt
            request_token="$(tr -d '[:space:]' < .github/dreamhost-preview-deploy-request.txt)"
            test "$request_token" = "DEPLOY_PREVIEW"
          fi

      - name: Validate required secrets
        shell: bash
        env:
          DREAMHOST_SSH_HOST: ${{ secrets.DREAMHOST_SSH_HOST }}
          DREAMHOST_SSH_PORT: ${{ secrets.DREAMHOST_SSH_PORT }}
          DREAMHOST_SSH_USER: ${{ secrets.DREAMHOST_SSH_USER }}
          DREAMHOST_SSH_PRIVATE_KEY: ${{ secrets.DREAMHOST_SSH_PRIVATE_KEY }}
          DREAMHOST_KNOWN_HOSTS: ${{ secrets.DREAMHOST_KNOWN_HOSTS }}
          DFW_PREVIEW_BASIC_AUTH_USER: ${{ secrets.DFW_PREVIEW_BASIC_AUTH_USER }}
          DFW_PREVIEW_BASIC_AUTH_PASS: ${{ secrets.DFW_PREVIEW_BASIC_AUTH_PASS }}
        run: |
          set -euo pipefail
          test -n "$DREAMHOST_SSH_HOST"
          test -n "$DREAMHOST_SSH_PORT"
          test -n "$DREAMHOST_SSH_USER"
          test -n "$DREAMHOST_SSH_PRIVATE_KEY"
          test -n "$DREAMHOST_KNOWN_HOSTS"
          test -n "$DFW_PREVIEW_BASIC_AUTH_USER"
          test -n "$DFW_PREVIEW_BASIC_AUTH_PASS"

      - name: Configure SSH
        shell: bash
        env:
          DREAMHOST_SSH_PRIVATE_KEY: ${{ secrets.DREAMHOST_SSH_PRIVATE_KEY }}
          DREAMHOST_KNOWN_HOSTS: ${{ secrets.DREAMHOST_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          install -m 700 -d ~/.ssh
          printf '%s\n' "$DREAMHOST_SSH_PRIVATE_KEY" > ~/.ssh/dreamhost_preview_key
          chmod 600 ~/.ssh/dreamhost_preview_key
          printf '%s\n' "$DREAMHOST_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: Upload and execute DreamHost deploy script
        shell: bash
        env:
          DREAMHOST_SSH_HOST: ${{ secrets.DREAMHOST_SSH_HOST }}
          DREAMHOST_SSH_PORT: ${{ secrets.DREAMHOST_SSH_PORT }}
          DREAMHOST_SSH_USER: ${{ secrets.DREAMHOST_SSH_USER }}
          DFW_PREVIEW_BASIC_AUTH_USER: ${{ secrets.DFW_PREVIEW_BASIC_AUTH_USER }}
          DFW_PREVIEW_BASIC_AUTH_PASS: ${{ secrets.DFW_PREVIEW_BASIC_AUTH_PASS }}
        run: |
          set -euo pipefail
          auth_user_b64="$(printf '%s' "$DFW_PREVIEW_BASIC_AUTH_USER" | base64 -w0)"
          auth_pass_b64="$(printf '%s' "$DFW_PREVIEW_BASIC_AUTH_PASS" | base64 -w0)"

          scp -i ~/.ssh/dreamhost_preview_key \
            -P "$DREAMHOST_SSH_PORT" \
            -o BatchMode=yes \
            -o StrictHostKeyChecking=yes \
            scripts/dreamhost-preview-deploy-remote.sh \
            "$DREAMHOST_SSH_USER@$DREAMHOST_SSH_HOST:/tmp/dfw-preview-deploy.sh"

          ssh -i ~/.ssh/dreamhost_preview_key \
            -p "$DREAMHOST_SSH_PORT" \
            -o BatchMode=yes \
            -o StrictHostKeyChecking=yes \
            "$DREAMHOST_SSH_USER@$DREAMHOST_SSH_HOST" \
            "DFW_PREVIEW_BASIC_AUTH_USER_B64='$auth_user_b64' DFW_PREVIEW_BASIC_AUTH_PASS_B64='$auth_pass_b64' LIVE_ROOT='$LIVE_ROOT' STAGING_ROOT='$STAGING_ROOT' PREVIEW_PATH='$PREVIEW_PATH' bash /tmp/dfw-preview-deploy.sh"

      - name: Validate DreamHost preview HTTP responses
        shell: bash
        env:
          DFW_PREVIEW_BASIC_AUTH_USER: ${{ secrets.DFW_PREVIEW_BASIC_AUTH_USER }}
          DFW_PREVIEW_BASIC_AUTH_PASS: ${{ secrets.DFW_PREVIEW_BASIC_AUTH_PASS }}
        run: |
          set -euo pipefail
          root_status="$(curl -s -o /dev/null -w '%{http_code}' https://dfwmatchdayconcierge.com/)"
          preview_public_status="$(curl -s -o /dev/null -w '%{http_code}' https://dfwmatchdayconcierge.com/preview/)"
          preview_auth_status="$(curl -s -o /dev/null -w '%{http_code}' -u "$DFW_PREVIEW_BASIC_AUTH_USER:$DFW_PREVIEW_BASIC_AUTH_PASS" https://dfwmatchdayconcierge.com/preview/)"
          connect_auth_status="$(curl -s -o /dev/null -w '%{http_code}' -u "$DFW_PREVIEW_BASIC_AUTH_USER:$DFW_PREVIEW_BASIC_AUTH_PASS" https://dfwmatchdayconcierge.com/preview/connect/)"

          echo "root_status=$root_status"
          echo "preview_public_status=$preview_public_status"
          echo "preview_auth_status=$preview_auth_status"
          echo "connect_auth_status=$connect_auth_status"

          test "$root_status" = "200"
          test "$preview_public_status" = "401"
          test "$preview_auth_status" = "200"
          test "$connect_auth_status" = "200"
